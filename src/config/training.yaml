training:
  seed: 42
  device: "cuda"

  epochs: 50
  batch_size: 4
  lr: 0.0005
  weight_decay: 0.0001
  optimizer: "adam"
  scheduler:
    type: "cosine"
    step_size: 10
    gamma: 0.5

  loss:
    type: "cross_entropy"
    class_weights: null
